package org.usfirst.frc.team1247.robot.utils;

import org.usfirst.frc.team1247.robot.RobotMap;

/**
 * An <code>enum</code> for the various states the lift system will be in.
 * Also contains the deltaX values for each state.
 * @author Zander Blasingame
 * @version v1.0.0
 */
/*public enum LiftingState {
	MANUAL_UP(RobotMap.UP_DELTA_X), 
	MANUAL_DOWN(RobotMap.DOWN_DELTA_X), 
	TOTE_UP(RobotMap.UP_DELTA_X),
	TOTE_DOWN(RobotMap.DOWN_DELTA_X), 
	CAN_UP(RobotMap.UP_DELTA_X), 
	CAN_DOWN(RobotMap.DOWN_DELTA_X), 
	NONE(0);
	
	private final int dx;
	
	private LiftingState(int dx) {
		this.dx = dx;
	}
	
	public int getDeltaX() {
		return dx;
	}
}
*/